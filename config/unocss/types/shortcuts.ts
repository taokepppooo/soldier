import type { UserShortcuts } from '@unocss/core'
export const shortcuts = {
  'sc-flex-xy-center': 'items-center justify-center', // 上下左右居中
  'sc-flex-y-center-x-left': 'flex items-center justify-start', // 上下居中，居左
} as UserShortcuts<{}>
